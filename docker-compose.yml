version: "3.9"

services:
  chess-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chess-app
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      NODE_ENV: production
    restart: unless-stopped
